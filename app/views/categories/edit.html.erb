<nav class="nav-bar">
  <ul>
    <li><%= link_to "Home", categories_path %></li>
    <li><%= link_to "Delete ", category_path(@category),
    method: :delete, data: { turbo_confirm: "Are you sure?" }%></li>
  </ul>
</nav>

<div class="edit">
  <h2>Edit Category</h2>

  <%= render "form", category: @category %>

  <%= link_to "Back to category", category_path(@category.id) %>
</div>
